stages:
  - build
  - deploy

variables:
  HUGO_VERSION: "0.136.2"

build:
  stage: build
  tags:
    - exec
  script:
    - make

push-remote:
  stage: deploy
  tags:
    - exec
  dependencies:
    - build
  script:
    - make push-remote
  only:
    - master